#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: first integer parameter
 * @b: second integer parameter
 * Return: results of add(int, int)
*/
int add(int a, int b)
{
	int results;

	results = a + b;

	return (results);
}
